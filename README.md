# SaveConfe - Система родительского контроля

SaveConfe — это десктоп-приложение для родительского контроля на Windows, позволяющее блокировать сайты и приложения, устанавливать лимиты времени использования и отслеживать активность.

## 🚀 Возможности

- 🔒 Блокировка сайтов и приложений
- ⏰ Установка лимитов времени использования
- 📊 Мониторинг активности и отчёты
- 🔐 Защита паролем администратора
- 🔔 Уведомления о приближении к лимиту
- 📤 Экспорт отчётов в CSV/Excel
- 🚀 Автозапуск при старте Windows
- 🔄 Работа в системном трее

## 📋 Требования

- Python 3.9+
- MySQL 8.0+
- Windows 10/11

## 🔧 Установка

### 1. Требования

- Windows 10/11
- Python 3.9 или выше
- MySQL 8.0 или выше

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка MySQL

#### Автоматическое создание базы данных (рекомендуется)

1. Убедитесь, что MySQL запущен
2. Скопируйте файл `database.env.example` в `database.env`:
```bash
copy database.env.example database.env
```
3. Откройте `database.env` и укажите ваши данные MySQL:
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=ваш_пароль_root
DB_NAME=saveconfe
```

4. Запустите скрипт создания базы данных:
```bash
python setup_database.py
```

Скрипт автоматически:
- Подключится к MySQL
- Создаст базу данных `saveconfe`
- Создаст все необходимые таблицы

#### Ручное создание базы данных

Если вы предпочитаете создать базу данных вручную:

1. Подключитесь к MySQL:
```bash
mysql -u root -p
```

2. Создайте базу данных:
```sql
CREATE DATABASE saveconfe CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

### 4. Запуск приложения

```bash
python main.py
```

**ВАЖНО:** При первом запуске приложение автоматически запросит права администратора. Это необходимо для блокировки сайтов через файл hosts.

При первом запуске будет предложено создать пароль администратора:
- Имя пользователя: `admin`
- Пароль: `admin`

**ВАЖНО:** Смените пароль после первого входа!

## 🎮 Использование

### Запуск приложения

```bash
python main.py
```

При первом запуске будет предложено создать пароль администратора.

### Основные функции

1. **Главная** — включение/отключение блокировки
2. **Сайты** — управление списком заблокированных сайтов
3. **Приложения** — управление списком заблокированных приложений
4. **Время** — настройка лимитов времени использования
5. **Отчёты** — просмотр активности пользователя
6. **Настройки** — настройка пароля, автозапуска и уведомлений

## 📦 Сборка EXE

Для создания исполняемого файла используйте:

```bash
build_exe.bat
```

Или вручную:

```bash
pip install pyinstaller
pyinstaller --onefile --noconsole --name SaveConfe --uac-admin --icon=resources/icons/icon.ico main.py
```

**ВАЖНО:** Флаг `--uac-admin` обеспечивает автоматический запрос прав администратора при запуске EXE.

Готовый EXE будет находиться в папке `dist/`.

## 🧪 Тестирование

```bash
pytest tests/
```

## 🔒 Безопасность

- Пароли хранятся в хешированном виде (bcrypt)
- Требуется авторизация для изменения настроек
- Защита от закрытия приложения (работа в фоне)
- Автоматический запрос прав администратора при запуске

## 🔐 Права администратора

Приложение автоматически запрашивает права администратора при запуске. Это необходимо для:
- Блокировки сайтов через файл hosts
- Завершения заблокированных процессов
- Работы с системными настройками

Если права администратора не предоставлены, блокировка сайтов работать не будет.

## 📝 Структура проекта

```
SaveConfe/
├── main.py                 # Точка входа
├── core/                   # Основная логика
│   ├── database.py        # Работа с MySQL
│   ├── auth.py            # Авторизация
│   ├── blocker.py         # Блокировка сайтов и приложений
│   ├── scheduler.py       # Планировщик времени
│   ├── monitor.py         # Мониторинг процессов
│   ├── autostart.py       # Автозапуск
│   └── admin_check.py     # Проверка прав администратора
├── ui/                     # Интерфейс
│   ├── main_window.py     # Главное окно
│   └── login_window.py    # Окно входа
├── models/                 # Модели данных
│   ├── user.py            # Пользователи
│   ├── site_rule.py       # Правила блокировки сайтов
│   ├── app_rule.py        # Правила блокировки приложений
│   └── usage_log.py       # Логи использования
├── resources/              # Ресурсы
│   └── styles.qss         # Стили интерфейса
├── tests/                  # Тесты
└── requirements.txt        # Зависимости
```

## 🛠️ Устранение неполадок

### Ошибка подключения к MySQL

- Проверьте, что MySQL запущен
- Проверьте настройки в `database.env`
- Убедитесь, что база данных создана

### Ошибка записи в hosts файл

- Запустите приложение от имени администратора
- Проверьте права доступа к файлу `C:\Windows\System32\drivers\etc\hosts`

### Приложение не запрашивает права администратора

- Убедитесь, что используете последнюю версию
- При запуске из исходников права запрашиваются автоматически
- При запуске EXE файла убедитесь, что он собран с флагом `--uac-admin`

### База данных не создаётся автоматически

- Убедитесь, что установлен `pymysql`: `pip install pymysql`
- Проверьте настройки подключения в `database.env`
- Запустите `python setup_database.py` вручную

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

SaveConfe Team

